<%= render "header" %>

<p id="notice"></p>

<div class="container">

<!--ジャンル検索-->
    <div class="col-md-2">
        <%= render "public/items/sidebar", genres: @genres %>
    </div>

<!--センター-->
    <div class="text-center mx-auto col-11 col-sm-10 col-lg-8 col-xl-6 border border-dark">
      <br>
      ようこそ、ながのCAKEへ！<br>
      このサイトは、ケーキ販売のECサイトになります。<br>
      会員でない方も商品の閲覧はできますが、<br>
      購入には会員登録が必要になります。
      <br><br>
    </div>
    <br>

<!--新着商品-->
  <h3 class="font-weight-bold">新着商品</h3>
      <div class="row font-weight-bold">
        <% @items.last(4).reverse.each do |item| %>
          <div class="col-3">
            <%= link_to(item) do %>
              <% if item.image.attached? %>
                <%= image_tag item.image, size: "150x100" %>
              <% else %>
                <%= image_tag 'no_image.jpg', size: "150x100" %>
              <% end %>
            <% end %>
            <div><%= item.name %></div>
          <p>￥<%= item.taxin_price.round.to_s(:delimited) %></p>
          </div>
          <% end %>
          </div>

        <div class="background right">
        <%= link_to "全ての商品を見る>", items_path %>
        </div>

      </div>
    </div>
</div>
<%= render "layouts/footer" %>