<div class="container">
  <h4 style="background-color:#EEEEEE;color:dark;display:inline">注文履歴詳細</h4>
   <table class="table">
    <tr>
      <td>購入者</td>
      <td><%= @order.customer.name %></td>
    </tr>
    <tr><td>注文日</td>
        <td><%= @order.created_at.strftime('%Y/%m/%d/ %H:%M:%S') %></td>
    </tr>
    <tr>
      <td>配送先</td>
      <td><%= @order.postal_code %> <%= @order.address %> <%= @order.name %></td>
    </tr>
    <tr>
      <td>支払い方法</td>
      <td><%= @order.payment_method_i18n %></td>
    </tr>
    <tr>
      <td>注文ステータス</td>
      <td>
        <%= form_with model: @order, url: admin_order_path(@order.id), method: :patch do |f| %>
        <%#= f.select :プロパティ名, options_from_collection_for_select(モデル, :フォームで送る値(メソッド), :セレクトボックスに表示する値(メソッド)) %>
        <%#= f.select :status, options_from_collection_for_select(Order.status :id, :name) %！考え中です！>
      </td>
    </tr>
</table>
</div>